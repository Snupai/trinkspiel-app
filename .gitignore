.gradle/
.kotlin/
build/
*/build/
local.properties

# Local release signing material. Keep real credentials out of version control.
signing/*.keystore
signing/*.jks
signing/release-signing.properties
